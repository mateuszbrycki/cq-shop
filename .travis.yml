language: java
jdk:
  - oraclejdk11
install:
  - ./cq-common/gradlew -b cq-common/build.gradle jar
  - ./user-management-service/gradlew -b user-management-service/build.gradle build
script:
  - ./user-management-service/gradlew -b user-management-service/build.gradle test
branches:
  only:
  - master
notifications:
  email: true